node_modules
dist
.env
.env.local
.claude/
.vercel

# Skill tooling (no forma parte de RAW)
.agents/
skills-lock.json
.env.bak-*
supabase/.temp/

# Salida del servidor de desarrollo: cambia en cada arranque y no aporta nada
# al repo. Trababa cada push con un conflicto en un archivo de log.
dev.log
