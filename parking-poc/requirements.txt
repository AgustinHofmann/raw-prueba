# Parking POC — pinned dependencies (Python 3.11)
# CPU builds of torch/torchvision; swap to a CUDA build if a GPU is present.

# Detection
ultralytics==8.3.49
torch==2.5.1
torchvision==0.20.1

# CV / geometry
numpy==1.26.4
opencv-python==4.10.0.84
shapely==2.0.6
PyYAML==6.0.2

# API / dashboard (used from M6)
fastapi==0.115.6
uvicorn[standard]==0.34.0
